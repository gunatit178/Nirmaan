# Only the OS (app/) and the agent specs go into the image.
*
!app
!agents
!Dockerfile.os
app/node_modules
app/.next
app/.env*
app/prisma/*.db
app/prisma/*.db-journal
# iCloud duplicates ("file 2.ts")
**/* [0-9]
**/* [0-9].*
