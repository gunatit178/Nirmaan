# Agency OS and its internal artifacts are not part of the public site.
# See docs/architecture-plan.md: /app is a separate, auth-gated deploy
# target that should never be reachable from the public marketing site.
app/
agents/
docs/
projects/
scripts/

# The OS deploy (Fly.io) is not part of the website.
Dockerfile.os
fly.toml
.dockerignore
