# Where the local database lives (relative to app/prisma). Not a secret.
# Secrets and keys go in .env.local, which git ignores. On the server,
# DATABASE_URL is set to a file on the persistent disk instead.
DATABASE_URL="file:./dev.db"
